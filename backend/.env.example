# Copy to .env. Postgres connection string for local development —
# matches the docker-compose `db` service published on host port 5433.
DATABASE_URL="postgresql://postgres:postgres@localhost:5433/cryptonext"
